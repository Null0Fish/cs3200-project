.DS_Store
secrets.toml
__pycache__
secrets/
.env
.env.local
*/logs/*
.vscode/
.venv/

# Uploaded clip videos. They land here because docker-compose.yaml bind-mounts
# ./api into the API container; they are user content, not source.
api/clip-files/
